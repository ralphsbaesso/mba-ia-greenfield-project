# Application
NODE_ENV=development
PORT=3000
# Base público da API, usado nos links dos e-mails transacionais
APP_URL=http://localhost:3000
# Swagger UI em /api. Default do schema é `false`, então precisa ser explícito
SWAGGER_ENABLED=true

# Database (PostgreSQL)
DB_HOST=db
DB_PORT=5432
DB_USERNAME=streamtube
DB_PASSWORD=streamtube
DB_NAME=streamtube

# JWT
JWT_SECRET=change-me-in-production
JWT_REFRESH_SECRET=change-me-refresh-in-production
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
CONFIRMATION_TOKEN_EXPIRATION_HOURS=1
PASSWORD_RESET_TOKEN_EXPIRATION_HOURS=1

# Mail (Mailpit for local development)
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_FROM='"StreamTube" <noreply@streamtube.com>'

# Object Storage (MinIO in local development, any S3-compatible provider elsewhere)
STORAGE_ENDPOINT=http://minio:9000
STORAGE_REGION=us-east-1
STORAGE_ACCESS_KEY=streamtube
STORAGE_SECRET_KEY=streamtube
STORAGE_BUCKET=streamtube

# Queue (Redis / BullMQ)
REDIS_HOST=redis
REDIS_PORT=6379
